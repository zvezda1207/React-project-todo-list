# 📝 Todo List - Приложение для управления задачами

Современное веб-приложение для управления списком задач, созданное на React с использованием Vite. Приложение позволяет создавать, редактировать, удалять и фильтровать задачи с автоматическим сохранением в локальном хранилище браузера.

## ✨ Основные возможности

- ✅ **Добавление задач** - создание новых задач через поле ввода
- ✏️ **Редактирование задач** - двойной клик по задаче для редактирования
- 🗑️ **Удаление задач** - удаление отдельных задач
- ☑️ **Отметка выполнения** - переключение статуса задачи (выполнена/не выполнена)
- 🔍 **Фильтрация** - просмотр всех, активных или выполненных задач
- 📊 **Счетчик задач** - отображение количества выполненных задач
- 🧹 **Очистка** - массовое удаление всех выполненных задач
- 💾 **Автосохранение** - автоматическое сохранение всех изменений в localStorage

## 🚀 Быстрый старт

### Требования

- Node.js (версия 18 или выше)
- npm или yarn

### Установка

1. Клонируйте репозиторий или скачайте проект
2. Установите зависимости:

```bash
npm install
```

### Запуск приложения

Для запуска в режиме разработки:

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist/`

### Просмотр собранной версии

```bash
npm run preview
```

## 📖 Инструкция по использованию

### Добавление задачи

1. Введите текст задачи в поле ввода в верхней части приложения
2. Нажмите кнопку "Добавить" или клавишу Enter
3. Задача появится в списке

### Редактирование задачи

1. **Двойной клик** по тексту задачи
2. Поле ввода заменит текст задачи
3. Внесите изменения
4. Сохраните одним из способов:
   - Нажмите **Enter** для сохранения
   - Кликните вне поля ввода (автосохранение)
   - Нажмите **Escape** для отмены редактирования

### Отметка задачи как выполненной

- Кликните по **чекбоксу** слева от задачи
- Выполненные задачи отображаются с зачеркнутым текстом

### Удаление задачи

- Нажмите кнопку **🗑** справа от задачи

### Фильтрация задач

Используйте кнопки фильтров внизу списка:
- **Все** - показать все задачи
- **Активные** - показать только невыполненные задачи
- **Выполненные** - показать только выполненные задачи

### Очистка выполненных задач

- Нажмите кнопку **"Очистить завершенные"** для удаления всех выполненных задач

## 🛠 Технологии

- **React 19.2.0** - библиотека для создания пользовательских интерфейсов
- **Vite 7.2.4** - инструмент сборки и разработки
- **localStorage** - для сохранения данных в браузере
- **ESLint** - для проверки качества кода

## 📁 Структура проекта

```
todo-list/
├── public/              # Статические файлы
├── src/
│   ├── assets/
│   │   └── components/
│   │       └── TodoList.jsx  # Основной компонент приложения
│   ├── App.jsx          # Главный компонент приложения
│   ├── App.css          # Стили приложения
│   ├── index.css        # Глобальные стили
│   └── main.jsx         # Точка входа
├── package.json         # Зависимости и скрипты
├── vite.config.js       # Конфигурация Vite
└── README.md            # Документация
```

## 🔧 Основные функции компонента

### Состояния (State)

- `todos` - массив всех задач
- `inputValue` - значение поля ввода новой задачи
- `filter` - текущий фильтр ('all', 'active', 'completed')
- `editingId` - ID задачи в режиме редактирования (null если нет)
- `editValue` - значение поля редактирования

### Основные функции

- `handleAddTodo()` - добавление новой задачи
- `handleToggle(id)` - переключение статуса задачи
- `handleDelete(id)` - удаление задачи
- `handleClearCompleted()` - удаление всех выполненных задач
- `startEditing(id, text)` - начало редактирования задачи
- `handleSaveEdit(id)` - сохранение изменений задачи
- `handleCancelEdit()` - отмена редактирования
- `saveToLocalStorage(todos)` - сохранение в localStorage

## 💾 Хранение данных

Все задачи автоматически сохраняются в **localStorage** браузера. Данные сохраняются при:
- Добавлении новой задачи
- Редактировании задачи
- Изменении статуса задачи
- Удалении задачи
- Очистке выполненных задач

Данные сохраняются в формате JSON и автоматически загружаются при открытии приложения.

## ⌨️ Горячие клавиши

- **Enter** (в поле редактирования) - сохранить изменения
- **Escape** (в поле редактирования) - отменить редактирование

## 🎯 Особенности реализации

- **Контролируемые компоненты** - все поля ввода связаны с состоянием React
- **Условный рендеринг** - переключение между режимом просмотра и редактирования
- **Автофокус** - автоматическая установка фокуса при начале редактирования
- **Валидация** - предотвращение добавления пустых задач
- **Оптимизация** - использование функциональных обновлений состояния

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 👨‍💻 Разработка

Для разработки используйте:

```bash
npm run dev    # Запуск dev-сервера
npm run lint   # Проверка кода линтером
```

---

**Приятного использования! 🎉**
